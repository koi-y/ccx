import * as t from "io-ts";

import { internalProjectEntity } from "../codecs";

type InternalProjectEntity = t.TypeOf<typeof internalProjectEntity>;

// eslint-disable-next-line no-undef
export default InternalProjectEntity;
