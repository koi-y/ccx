import * as t from "io-ts";

import { internalHistoryEntity } from "../codecs";

type InternalHistoryEntity = t.TypeOf<typeof internalHistoryEntity>;

// eslint-disable-next-line no-undef
export default InternalHistoryEntity;
