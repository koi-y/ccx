import * as t from "io-ts";

import { internalProjectSource } from "../codecs";

type InternalProjectSource = t.TypeOf<typeof internalProjectSource>;

// eslint-disable-next-line no-undef
export default InternalProjectSource;
