import * as t from "io-ts";

import { projectLastUpdated } from "../codecs";

type ProjectLastUpdated = t.TypeOf<typeof projectLastUpdated>;

// eslint-disable-next-line no-undef
export default ProjectLastUpdated;
