import * as t from "io-ts";

import { projectRevision } from "../codecs";

type ProjectRevision = t.TypeOf<typeof projectRevision>;

// eslint-disable-next-line no-undef
export default ProjectRevision;
